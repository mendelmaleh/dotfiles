#!/bin/sh

if command -v spotifyd &> /dev/null && ! pidof spotifyd > /dev/null; then
    user="$(pass spotify/user)" 
    password="$(pass spotify/$user)" 
    spotifyd --no-daemon -d $HOSTNAME -u $user -p $password >/dev/null &
fi

export PATH=${PATH#$HOME/bin:}; spt $@
